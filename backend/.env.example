# Copy to .env (never commit real credentials). All values have local defaults
# in application.yml; only overrides are needed here.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=resumeflow
DB_USERNAME=postgres
DB_PASSWORD=change-me
REDIS_HOST=localhost
REDIS_PORT=6379
RESUMEFLOW_STORAGE_PATH=./storage
MAX_RESUME_SIZE_MB=10
